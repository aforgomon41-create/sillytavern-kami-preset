# 并行会话协作备忘（已归档）

> **2026-09-20 深夜：用户确认会话 A 已收工，并行限制解除。**
> 本文件转为历史记录保留；构建占用/浏览器占用的登记约定随之作废。
> 后续会话不需要再读本文件（docs/README.md 索引可酌情移除这一行）。

> 这个文件是**两个并行会话的协调点**，不是知识文档（知识仍按 docs/README.md 的约定归三份必读）。
> 谁来了谁登记，走之前把自己的段落更新成最终状态。并行作业结束后这份文件可以由主线会话归档删除。

---

## 当前并行会话

### 会话 B：压缩脚本（📜 压缩 / 大总结）（登记时间 2026-09-20 深夜，状态：**v0.2 代码完成（一块一条目+深度锚定），停在构建前等用户点头**）

**任务**：开发压缩脚本「📜 压缩」。两个模式并存：滚动压缩（面板里管正则「压缩|摘要/设定」的开关与起始深度）+ 超限压缩（发送 token 达上限或手动触发，把保留范围外的楼层分块总结成"压缩块"存进**聊天世界书**，被总结楼层用酒馆原生隐藏标记隐藏）；另附「🧱 摘要(小总结)」条目状态卡。面板骨架复用 40-预设设置.js 的面板（.kami-* 契约类 + 手势共享模块 + 内联兜底皮肤）。

**我认领的文件（其它会话请勿改动）**：
- `docs/协作备忘.md`（本文件）
- `src/scripts/60-压缩.js`（新脚本）
- `src/scripts/meta.json` 里**追加的一条登记**（只加自己的条目，不碰别人的）
- `docs/大总结功能.md`（功能说明）
- `docs/README.md` 索引表里**追加的一行**（指向本文件）

**我对共享资源的承诺**：
1. **不碰真机浏览器**：不开浏览器会话、不借用酒馆标签页、不部署产物。真机验证全部由用户下载产物自行实测。
2. **不擅自构建**：构建前先在下面「构建占用」栏登记并确认栏位为空；构建完立刻释放。构建动作本身只写 dist/，不动 src/。
3. **共享文件零改动**：除上述认领文件外不改任何文件（包括 交接.md / 皮肤契约.md / base.css / harness / 皮肤包）。我的面板只用现有契约类与面板 ID 作用域，不需要动皮肤与兜底皮肤。
4. 预设文件（src/preset.base.json）**绝不修改**：面板对正则与条目的写入全部走运行时活设置通道。

### 会话 A：（主线会话，请在此追加你的段落）

- 正在做什么、占用什么文件、什么时候打算构建，写在这里。
- 「构建占用」栏谁用谁登记，用完清空。

---

## 构建占用登记（用完清空）

| 时间 | 会话 | 产物号 | 状态 |
|---|---|---|---|
| （空） | | | |

## 浏览器占用登记（真机酒馆标签页，用完清空）

| 时间 | 会话 | 用途 | 状态 |
|---|---|---|---|
| （空） | | | |
