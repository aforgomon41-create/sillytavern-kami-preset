# 卡密预设 v0.9 · 预设结构解析表

> 本文档由 `test/harness/preset-report.mjs` 自动生成：先在**内存里**跑迁移（`migratePreset`），再把迁移后的对象交给**纯函数解析器**（`parsePreset`），最后把结果铺成这张表。
> 
> **`src/preset.base.json` 已经是迁移后的状态**（167 条 prompts / order 163 项）。所以迁移断言是从**迁移前备份** `dist/_preset.base.迁移前备份.json`（299126 字符）在内存里重跑出来的，并核对「重跑结果 == 盘上的 base」：**不一致（见第 2 节）**。解析用的始终是盘上的 base。

| 项目 | 值 |
| --- | --- |
| 预设文件 | `src/preset.base.json`（305629 字符） |
| 预设内条目总数 | prompts 165 条 → 迁移后 167 条 |
| order 数组（`prompt_order[0].character_id=100001`） | 163 项（迁移后） |
| 面板主键 | 完整 `identifier`（前 8 位有 102 条重名成 `prompt_1`，所以短号取**后 6 位**） |
| 解析入口 | `test/harness/preset-parse.mjs` → `parsePreset(preset)`（零 import、无正则，可直接给面板用） |

## 0. 一句话结论

迁移 4 处全部通过断言；解析出 **6 个层级 tab + 1 个全局「🤖 模型」tab**、共 **7 个 tab**、**21 张层级卡片 + 4 张模型卡片**、**99 条可开关内容条目**。
本轮按用户裁定把 **规则 15（模型专属卡片降级）收窄**：现在**只有「某张卡片是它所在层级里唯一的卡片」且名下条目全是模型专属条目**时才降级（旧版不看"唯一卡片"）。全预设 22 张层级卡片里：**1 张**命中（`🤖 [模型类型]`（在 🪓 神秘咒语 层级））→ `🪓 神秘咒语` 变成「无卡片无变量」→ 按规则 8 **不出 tab**；另有 **1 张**名下条目也全是模型专属，但**不是该层里唯一的卡片**（`🤔 [推理格式]`（在 🧠 推理选项 层级，该层共 5 张卡片））→ 按收窄后的规则**保持卡片身份**。**tab 数 7（层级 6 + 模型 1）**（见第 11 节）。
**没有任何条目被删掉**：被降级的卡片名下那 3 条模型专属条目照旧进「🤖 模型」tab（逐条核对见第 11.5 / 11.6 节）；被收窄放过的 `🤔 [推理格式]` 名下 2 条照旧是层级卡片里的条目（同时也镜像进模型 tab）。
面板上找不到的条目：**5 条** = 规则 4/8 原有的 4 条（全部来自 `📌 任务设置`，用户已确认接受）+ **规则 15 仍会引入的 1 条**（`🤖 [模型类型] 选一`：它所在的 `🪓 神秘咒语` 层级里**只有它这一张卡片**，所以收窄后照旧被判掉，而它的 content 只有一条 `{{// }}` 注释）→ **没有回到 4 条**，见第 9 节与第 11.7 节。

## 1. 迁移改动清单（共 4 处）

### A1 · 新增「✒️ [文风优化] 任选」卡片头（名字已定稿）

| 项 | 内容 |
| --- | --- |
| 位置 | order 第 77 位：紧跟 `📝 写作指导🔻` 之后、`💀 反死人文风(Gemini可不开)` 之前 |
| identifier | `prompt_kami_style_opt`（自造，已断言在 prompts 里唯一） |
| 原名 | （无，新增） |
| 新名 | `✒️ [文风优化] 任选` ← **用户已定稿**：emoji ✒️ = U+2712 + 变体选择符 U+FE0F（与预设里已有的 🖊️ / 🗣️ / ⚔️ 同一套写法）+ 人类可读名「文风优化」+ 后缀「 任选」 |
| 为什么不用 🪶 | U+1FAB6 是 Emoji 13.0 才加入的字符，部分设备会显示成方框；已改成 U+2712 + U+FE0F |
| 占用检查 | 全预设 165 条 prompts 的 name 里，U+2712（✒）出现 0 次（0 = 没有别的条目用过这个符号） |
| content 变化 | 新增，content = 空字符串 `''` |
| enabled | true |
| 字段集合 | content,enabled,forbid_overrides,identifier,injection_depth,injection_order,injection_position,marker,name,role,system_prompt（**照抄邻居** `📝 写作指导🔻`，未发明新字段） |
| 效果 | 它成为 `📝 写作指导` 层级里的第一张卡片（多选），吃掉紧随其后的 3 条：💀 反死人文风(Gemini可不开)、🔄 写作节奏优化、🔞 NSFW基础 |

### A2 · 新增「📋 支持模型列表 | model」模型登记表条目（名字已定稿）

| 项 | 内容 |
| --- | --- |
| 位置 | order 第 1 位：紧跟 `‼️免责声明/开源许可‼️(不开)` 之后，成为 order 里的第 2 条 |
| identifier | `prompt_kami_model_table`（自造，已断言在 prompts 里唯一） |
| 原名 | （无，新增） |
| 新名 | `📋 支持模型列表 &#124; model` ← **用户已定稿**：前缀 emoji 📋（U+1F4CB）+ 人类可读名「支持模型列表」+ 已确认后缀 ` &#124; model` |
| content 变化 | 新增，content = 从 `🤖 [模型类型] 选一` 里**逐字搬来**的那段 emoji 表 |
| enabled | true |
| 字段集合 | content,enabled,forbid_overrides,identifier,injection_depth,injection_order,injection_position,marker,name,role,system_prompt（**照抄邻居** `‼️免责声明/开源许可‼️(不开)`） |

搬过来的原文（含 `{{//` 与 `}}`、含换行，一字未改）：

```
{{//
🐋：表示DeepSeek系列模型
💤：表示GLM系列模型
🐱：表示Gemini系列模型
🌒：表示Kimi系列模型
}}
```

### A3 · 从「🤖 [模型类型] 选一」里删掉那张 emoji 表

| 项 | 内容 |
| --- | --- |
| 位置 | order 第 11 位：`🤖 [模型类型] 选一` |
| identifier | `prompt_1789213815769_ks7pgcc`（既有条目，未改） |
| 原名 / 新名 | `🤖 [模型类型] 选一` → **同名，一个字都没改** |
| content 变化 | 删掉第二个 `{{// … }}` 整块（那把 emoji 表），只保留第一个 `{{// … }}` 块 |
| 其他字段 | 一个字符都没动（它的字段集合比邻居多 4 个：injection_trigger,attach_role,attach_index,attach_side，原样保留） |

保留后的 content：

```
{{//不同模型需要不同的适配方法，条目名有一种emoji的即为该模型专属，选择某一模型时必须关闭其他模型专属条目。}}
```

### A4 · 三条变量条目改名（只在名字末尾加 ` | var`）

| 变量条目 | identifier | 原名 | 新名 | content 变化 |
|---|---|---|---|---|
| 🧩 推理预算 &#124; var | prompt_1788363449781_glre0pi | 🧩 推理预算 | 🧩 推理预算 &#124; var | 未改动 |
| 🧩 正文字数 &#124; var | prompt_1788363595936_1f5wx8e | 🧩 正文字数 | 🧩 正文字数 &#124; var | 未改动 |
| 🧩 插图数量 &#124; var | prompt_1788363831339_iql9vx8 | 🧩 插图数量 | 🧩 插图数量 &#124; var | 未改动 |

> 改名后 `🧩 设置变量` 层级里仍是 3 条变量条目、5 个变量（见第 5 节）。

## 2. 断言结果

### 2.1 两条硬断言（任务书点名要求）

**断言一 · 竖线撞车检查：通过（0 命中）**

- 检查范围：原文件全部 165 条 `prompts` 的 `name`（不是只看 order 里的 161 条）。
- 结果：迁移前 165 条 prompts 的 name 里都没有半角竖线 |，` | var` / ` | model` 后缀可安全启用
- 迁移后复审：迁移后含竖线的 name 共 4 条，全部是 ` | var`(3) 或 ` | model`(1)：📋 支持模型列表 | model / 🧩 推理预算 | var / 🧩 正文字数 | var / 🧩 插图数量 | var

**断言二 · 顺序一致性检查：通过**

- 结果：剔除两条新增后，order 里 161 条既有 identifier 的序列与原文件逐位相同
- 插入位校验：📋 支持模型列表 @ order[1]，✒️ [文风优化] 任选 @ order[77]
- 补充：除 A3 改的 1 条 + A4 改名的 3 条之外，其余 161 条 prompt 完全逐字节相同

### 2.2 全部断言明细

| # | 断言 id | 结果 | 说明 |
|---|---|---|---|
| 1 | style-roundtrip | PASS | 原文件 == JSON.stringify(obj, null, 2)（缩进 2 空格、字段顺序原样、无结尾换行），写回可保持原风格 |
| 2 | name-trim-audit | PASS | 名字比对前统一 trim()；原文件有 5 条条目名带前导空格 |
| 3 | no-pipe-in-names-before | PASS | 迁移前 165 条 prompts 的 name 里都没有半角竖线 &#124;，` &#124; var` / ` &#124; model` 后缀可安全启用 |
| 4 | new-id-style-unique | PASS | prompt_kami_style_opt 在 prompts 里唯一 |
| 5 | new-id-model-unique | PASS | prompt_kami_model_table 在 prompts 里唯一 |
| 6 | a1-name-codepoints | PASS | A1 名字以 U+2712 + U+FE0F（✒️）开头：U+2712 + U+FE0F |
| 7 | a1-emoji-unused-in-names | PASS | 全预设 165 条 prompts 的 name 里，U+2712（✒）出现 0 次（0 = 没有别的条目用过这个符号） |
| 8 | anchor-disclaimer-found | PASS | ‼️免责声明/开源许可‼️(不开) @ order[0] |
| 9 | anchor-writing-found | PASS | 📝 写作指导🔻 @ order[75] |
| 10 | target-model-card-found | PASS | 🤖 [模型类型] 选一 @ order[11] |
| 11 | a3-first-block-found | PASS | 第一段 {{// ... }}：{{//不同模型需要不同的适配方法，条目名有一种emoji的即为该模型专属，选择某一模型时必须关闭其他模型专属条目。}} |
| 12 | a3-second-block-found | PASS | 第二段 {{// ... }}（emoji 表）："{{//\n🐋：表示DeepSeek系列模型\n💤：表示GLM系列模型\n🐱：表示Gemini系列模型\n🌒：表示Kimi系列模型\n}}" |
| 13 | a3-rest-is-exactly-second-block | PASS | 第一段之后剩余的内容（去首尾空白后）就是那一段 emoji 表，可整段搬走 |
| 14 | a3-table-has-4-rows | PASS | emoji 表共 4 行（全角冒号分隔） |
| 15 | order-sequence-preserved | PASS | 剔除两条新增后，order 里 161 条既有 identifier 的序列与原文件逐位相同 |
| 16 | order-insert-positions | PASS | 📋 支持模型列表 @ order[1]，✒️ [文风优化] 任选 @ order[77] |
| 17 | other-entries-untouched | PASS | 除 A3 改的 1 条 + A4 改名的 3 条之外，其余 161 条 prompt 完全逐字节相同 |
| 18 | prompt-count | PASS | prompts: 165 → 167 |
| 19 | order-length | PASS | order: 161 → 163 |
| 20 | a1-field-set-match-neighbor | PASS | 新增 A1 字段集合 == 邻居 `📝 写作指导🔻`：content,enabled,forbid_overrides,identifier,injection_depth,injection_order,injection_position,marker,name,role,system_prompt |
| 21 | a2-field-set-match-neighbor | PASS | 新增 A2 字段集合 == 邻居 `‼️免责声明/开源许可‼️(不开)`：content,enabled,forbid_overrides,identifier,injection_depth,injection_order,injection_position,marker,name,role,system_prompt |
| 22 | table-moved-verbatim | PASS | 登记表内容与从 `🤖 [模型类型] 选一` 删掉的第二段完全一致 |
| 23 | a3-content-equals-first-block | PASS | 「模型类型 选一」content == 第一段注释 |
| 24 | post-pipe-only-convention | PASS | 迁移后含竖线的 name 共 4 条，全部是 ` &#124; var`(3) 或 ` &#124; model`(1)：📋 支持模型列表 &#124; model / 🧩 推理预算 &#124; var / 🧩 正文字数 &#124; var / 🧩 插图数量 &#124; var |
| 25 | enabled-consistency | PASS | order.enabled 与 prompts.enabled 不一致条目数：迁移前 1 / 迁移后 1 |
| 26 | index-alignment | PASS | out.prompts[i].identifier == out.prompt_order[0].order[i].identifier（前 163 条） |

## 3. 解析树（每个 tab → 每张卡片 → 条目数）

面板一共 **7 个 tab**：6 个来自「有卡片或有变量」的层级 + 1 个全局「🤖 模型」tab。

### 3.0 tab 总览

| # | tab | 来源层级 | 卡片数 | 卡片内条目 | 独立条目 | 变量条目 | 层级 order 区间 |
|---|---|---|---|---|---|---|---|
| 1 | 🧩 设置变量 | 🧩 设置变量🔻 … 🧩 设置变量🔺 | 0 | 0 | 2 | 3 | 2 … 8 |
| 2 | 📖 创作要求 | 📖 创作要求🔻 … 📖 创作要求🔺 | 9 | 31 | 0 | 0 | 23 … 64 |
| 3 | 😀 角色塑造 | 😀 角色塑造🔻 … 😀 角色塑造🔺 | 1 | 8 | 0 | 0 | 65 … 75 |
| 4 | 📝 写作指导 | 📝 写作指导🔻 … 📝 写作指导🔺 | 3 | 31 | 0 | 0 | 76 … 111 |
| 5 | 🧪 额外任务 | 🧪 额外任务🔻 … 🧪 额外任务🔺 | 3 | 6 | 0 | 0 | 112 … 122 |
| 6 | 🧠 推理选项 | 🧠 推理选项🔻 … 🧠 推理选项🔺 | 5 | 13 | 0 | 0 | 143 … 162 |
| 7 | 🤖 模型 | （全局特殊 tab，跨所有层级） | 4 | 12 | 0 | 0 | - |

### 3.1 🧩 设置变量

- 层级：`🧩 设置变量🔻` … `🧩 设置变量🔺`（order 2 … 8，两条层级条目本身**锁定为开、不出现在面板**）
- 规模：卡片 0 张 ｜ 卡片内条目 0 条 ｜ 独立条目 2 条 ｜ 变量条目 3 条

**独立条目（不属任何卡片）**

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🧩 清空变量 | prompt_1788364022825_s7ebevr | 开 | - | 用于每次请求构建上下文时清空变量，防止含变量的条目开关导致的变量残留 |
| 2 | 🧩 标签格式 | prompt_1788362900096_7ladf0n | 开 | - | 此处用来设置各种标签，可能有部分没有实际使用或并没有使用变量，慎改 |

### 3.2 📖 创作要求

- 层级：`📖 创作要求🔻` … `📖 创作要求🔺`（order 23 … 64，两条层级条目本身**锁定为开、不出现在面板**）
- 规模：卡片 9 张 ｜ 卡片内条目 31 条 ｜ 独立条目 0 条 ｜ 变量条目 0 条

**卡片：🗣️ [语言文字]（单选）** ｜ 卡片头 identifier `prompt_1788957769697_1ebfo2g` ｜ 当前 开 ｜ 条目 **4** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🗣️ 简体中文 | 1691ea8b-623b-4b71-8f22-b91b04b70241 | 开 | - | （无注释） |
| 2 | 🗣️ 繁體中文 | 15b8dc34-afa9-4c80-8311-e569655a0ed2 | 关 | - | （无注释） |
| 3 | 🗣️ English | 4c306bfa-1f19-4b25-8578-3b42717ccbe3 | 关 | - | （无注释） |
| 4 | 🗣️ 日本語 | 5412371f-2849-49d4-9e6a-0d61d5588ffa | 关 | - | （无注释） |
| 合计 | 共 4 条 | - | 开 1 / 关 3 | - | - |

**卡片：🏃🏻‍♂️ [剧情推进]（单选）** ｜ 卡片头 identifier `ce1700b1-3730-48ff-82f9-300ab2122b45` ｜ 当前 开 ｜ 条目 **3** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🏃🏻‍♂️ 快速推进 | prompt_1788958152723_dnu95j3 | 关 | - | （无注释） |
| 2 | 🏃🏻‍♂️ 详略得当 | prompt_1788957850425_qlibl5z | 开 | - | （无注释） |
| 3 | 🏃🏻‍♂️ 慢速推进 | prompt_1788958211748_jr6lsvp | 关 | - | （无注释） |
| 合计 | 共 3 条 | - | 开 1 / 关 2 | - | - |

**卡片：👁️ [叙事视角]（单选）** ｜ 卡片头 identifier `prompt_1788265586164_g6c61gb` ｜ 当前 开 ｜ 条目 **4** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 👁️ 限知第三人称 | prompt_1788265784251_4jtobgs | 开 | - | （无注释） |
| 2 | 👁️ 全知第三人称 | prompt_1788265969217_ovu6tx6 | 关 | - | （无注释） |
| 3 | 👁️ 第一人称 | prompt_1788265975192_c53pn3j | 关 | - | （无注释） |
| 4 | 👁️ 第二人称 | prompt_1788265974014_ayocskv | 关 | - | （无注释） |
| 合计 | 共 4 条 | - | 开 1 / 关 3 | - | - |

**卡片：🎮 [创作权限]（单选）** ｜ 卡片头 identifier `dde2979c-136c-49cf-aade-328546986f12` ｜ 当前 开 ｜ 条目 **3** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🎮 严禁抢话 | prompt_1788266431157_5a5evtb | 关 | - | （无注释） |
| 2 | 🎮 适当抢话 | prompt_1788266543344_1s6k607 | 开 | - | （无注释） |
| 3 | 🎮 鼓励抢话 | prompt_1788266541063_mvt1rti | 关 | - | （无注释） |
| 合计 | 共 3 条 | - | 开 1 / 关 2 | - | - |

**卡片：🔊 [转述程度]（单选）** ｜ 卡片头 identifier `prompt_1788266393909_xqfkh6q` ｜ 当前 开 ｜ 条目 **3** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🔊 严禁转述 | d8eab335-6c34-429c-8394-27349c141d03 | 关 | - | （无注释） |
| 2 | 🔊 适当转述 | f05c1943-3986-4be4-8606-38c11c97a651 | 开 | - | （无注释） |
| 3 | 🔊 扩写模式 | 8b07cc7e-6990-4f54-8162-e7d0a78fd199 | 关 | - | （无注释） |
| 合计 | 共 3 条 | - | 开 1 / 关 2 | - | - |

**卡片：⚔️ [难度设定]（单选）** ｜ 卡片头 identifier `prompt_1788266632069_v834ec2` ｜ 当前 开 ｜ 条目 **6** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | ⚔️ N?-拟真推理 | prompt_1788266687794_khf5ypg | 开 | - | （无注释） |
| 2 | ⚔️ N1-为所欲为 | prompt_1788266836387_8bavd9j | 关 | - | （无注释） |
| 3 | ⚔️ N2-轻松愉快 | prompt_1788266911896_mcj1opv | 关 | - | （无注释） |
| 4 | ⚔️ N3-均衡挑战 | prompt_1788266911511_0gd9v53 | 关 | - | （无注释） |
| 5 | ⚔️ N4-硬核挑战 | prompt_1788266910977_wop2wgb | 关 | - | （无注释） |
| 6 | ⚔️ N5-挑战地狱 | prompt_1788267029782_f9tno2f | 关 | - | （无注释） |
| 合计 | 共 6 条 | - | 开 1 / 关 5 | - | - |

**卡片：🎯 [叙事焦点]（单选）** ｜ 卡片头 identifier `prompt_1788267141636_9nbh32y` ｜ 当前 开 ｜ 条目 **4** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🎯 主角独奏 | prompt_1788267178577_gytcbbe | 关 | - | （无注释） |
| 2 | 🎯 主配角平衡 | prompt_1788267220973_7brwtsn | 开 | - | （无注释） |
| 3 | 🎯 群像剧 | prompt_1788267254036_e02kasu | 关 | - | （无注释） |
| 4 | 🎯 观察配角 | prompt_1788267298357_f755rwu | 关 | - | （无注释） |
| 合计 | 共 4 条 | - | 开 1 / 关 3 | - | - |

**卡片：💕 [色色选项]（多选）** ｜ 卡片头 identifier `prompt_1789212331071_e51w9xa` ｜ 当前 开 ｜ 条目 **2** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 💕 温柔色色 | prompt_1789212370832_gfayymh | 开 | - | 开启后引导亲密描写侧重美好舒适与情感交流，角色始终展现魅力；不开则完全由所选文风决定。 |
| 2 | 💕 多轮次色色 | prompt_1789212600589_2zsrda2 | 开 | - | 开启后将亲密剧情细分为调情、前戏、高潮等阶段，每轮仅推进一步且绝不跳步；不开则可能快速完事。 |
| 合计 | 共 2 条 | - | 开 2 / 关 0 | - | - |

**卡片：⚙️ [杂项要求]（多选）** ｜ 卡片头 identifier `92d01b6d-c60b-49d3-a291-8ee9b1aa25db` ｜ 当前 开 ｜ 条目 **2** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | ⚙️ 疯狂创意 | 425617fa-966e-4482-95ac-0482fc388c75 | 关 | - | （无注释） |
| 2 | ⚙️ 尾部仿出戏 | 5d4fcd99-d6bc-4a8d-9959-981186207535 | 开 | - | 开启后严禁在正文末尾附加提问、说教或以主角言行收尾，保持沉浸；不开则结尾可能出现旁白出戏。 |
| 合计 | 共 2 条 | - | 开 1 / 关 1 | - | - |

### 3.3 😀 角色塑造

- 层级：`😀 角色塑造🔻` … `😀 角色塑造🔺`（order 65 … 75，两条层级条目本身**锁定为开、不出现在面板**）
- 规模：卡片 1 张 ｜ 卡片内条目 8 条 ｜ 独立条目 0 条 ｜ 变量条目 0 条

**卡片：🚫 [各种防止]（多选）** ｜ 卡片头 identifier `prompt_1788268012256_g71qv8b` ｜ 当前 开 ｜ 条目 **8** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🚫 防全知 | prompt_1788269003013_jw48alm | 开 | - | 开启后角色仅能根据亲眼所见与亲耳所闻行动，杜绝超游掌握场外信息；不开则角色可能未卜先知。 |
| 2 | 🚫 防绝望 | prompt_1788270585296_2cc9a3w | 开 | - | 开启后角色性格更坚韧阳光，防止陷入极端偏执或过度绝望；不开则角色负面情绪可能较剧烈。 |
| 3 | 🚫 防OOC | prompt_1788269001893_tijbjo2 | 开 | - | 开启后仅根据已有设定描写角色言行，严禁随意脑补；不开则角色性格可能逐渐偏离设定。 |
| 4 | 🚫 防发情 | prompt_1788269188169_kc1mob5 | 关 | - | 开启后角色不会无缘无故主动发情，仅在氛围合适时切入亲密剧情；不开则可能在日常中也频繁多情。 |
| 5 | 🚫 防冰冷 | prompt_1788269211741_dzafvas | 开 | - | 开启后禁止机械化、报数据或报告式的僵硬对话，保持情感充沛；不开则可能输出冷冰冰的分析腔调。 |
| … | 其余 3 条：🚫 防神化 / 🚫 防讨好 / 🚫 防说教 | - | - | - | - |
| 合计 | 共 8 条 | - | 开 7 / 关 1 | - | - |

### 3.4 📝 写作指导

- 层级：`📝 写作指导🔻` … `📝 写作指导🔺`（order 76 … 111，两条层级条目本身**锁定为开、不出现在面板**）
- 规模：卡片 3 张 ｜ 卡片内条目 31 条 ｜ 独立条目 0 条 ｜ 变量条目 0 条

**卡片：✒️ [文风优化]（多选）** ｜ 卡片头 identifier `prompt_kami_style_opt` ｜ 当前 开 ｜ 条目 **3** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 💀 反死人文风(Gemini可不开) | 7b65a2e1-fbc9-4f36-8e48-f43aade44048 | 开 | - | （无注释） |
| 2 | 🔄 写作节奏优化 | 7db02e6b-8594-48c7-bb7b-9f0df018e677 | 开 | - | （无注释） |
| 3 | 🔞 NSFW基础 | prompt_1789211733662_xia9ii1 | 开 | - | （无注释） |
| 合计 | 共 3 条 | - | 开 3 / 关 0 | - | - |

**卡片：🖊️ [写作文风]（单选）** ｜ 卡片头 identifier `prompt_1789202230919_g831g9s` ｜ 当前 开 ｜ 条目 **23** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🌊 流畅行文 | prompt_1789205511889_x5uy830 | 关 | - | （无注释） |
| 2 | 🌐 通用网文 | prompt_1789206534598_s3wkh46 | 关 | - | （无注释） |
| 3 | 😄 搞笑网文 | prompt_1789205543223_mtc6o3z | 关 | - | （无注释） |
| 4 | 🎆 细腻网文 | prompt_1789205542237_xmdwqcu | 关 | - | （无注释） |
| 5 | 🎬 电影化叙事 | prompt_1789205694462_jqftq9v | 关 | - | （无注释） |
| … | 其余 18 条：🗿 海明威 / 😘 萌系轻小说 / 🎭 幽默轻小说 / 😔 细腻轻小说 / 🪔 古风言情 / ⚔️ 托尔金&马丁 / 🌌 刘慈欣 / 🔪 古龙武侠 / 👊🏻 金庸武侠 / 💗 柚子社gal / 😭 Key社gal / 🔫 Nitro gal / 🦇 爱伦坡 / 🐙 洛夫克拉夫特 / 📕 古典章回体 / 👻 古典志怪聊斋 / 😏 古典世情讽刺 / 🌺 古典风花雪月 | - | - | - | - |
| 合计 | 共 23 条 | - | 开 1 / 关 22 | - | - |

**卡片：🔞 [NSFW文风]（单选）** ｜ 卡片头 identifier `prompt_1789210991528_foithxs` ｜ 当前 开 ｜ 条目 **5** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | ❌ 不开 | 81c7d8a2-4ab7-4d7c-8668-3102653a41e9 | 关 | - | （无注释） |
| 2 | 🔞 擦边 | prompt_1789211909290_wu2t72x | 关 | - | （无注释） |
| 3 | 🔞 唯美浪漫 | prompt_1789212169309_tafxj83 | 关 | - | （无注释） |
| 4 | 🔞 情欲交加 | prompt_1789212168791_ed1odxe | 关 | - | （无注释） |
| 5 | 🔞 粗俗直白 | prompt_1789212166772_pp243yy | 开 | - | （无注释） |
| 合计 | 共 5 条 | - | 开 1 / 关 4 | - | - |

### 3.5 🧪 额外任务

- 层级：`🧪 额外任务🔻` … `🧪 额外任务🔺`（order 112 … 122，两条层级条目本身**锁定为开、不出现在面板**）
- 规模：卡片 3 张 ｜ 卡片内条目 6 条 ｜ 独立条目 0 条 ｜ 变量条目 0 条

**卡片：🔝 [头部任务]（多选）** ｜ 卡片头 identifier `prompt_1789213927315_4s0dsfk` ｜ 当前 开 ｜ 条目 **1** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 占位(暂时只有Gemini的显式思维链) | 72e10746-4da8-4c7f-a0f6-57d9c01a8113 | 关 | - | （无注释） |
| 合计 | 共 1 条 | - | 开 0 / 关 1 | - | - |

**卡片：🔛 [正文任务]（多选）** ｜ 卡片头 identifier `92d8d108-bff1-4f46-88d3-97875f765f76` ｜ 当前 开 ｜ 条目 **2** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🎥 HTML美化 | d1087e89-87e4-4390-8e3c-a0120473dc1c | 开 | - | 开启后将剧情里的书信、终端、短信等载体渲染为美观的 HTML 卡片；不开则仅显示纯文本。 |
| 2 | 🎨 文生图 | prompt_1789310982400_v7fihu3 | 关 | - | 开启后在正文中插入分镜插图标签，方便生图插件自动绘制配图；不开则不生成插图标签。 |
| 合计 | 共 2 条 | - | 开 1 / 关 1 | - | - |

**卡片：🔚 [尾部任务]（多选）** ｜ 卡片头 identifier `eac324bd-b9ba-4623-97f6-e35ffc20ca15` ｜ 当前 开 ｜ 条目 **3** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🧱 摘要(小总结) | 3fd5251a-b51b-4fc2-a4a8-fbb955faa527 | 开 | - | 开启后在尾部生成短摘要供长上下文管理，防止剧情遗忘并节省字数消耗；不开则不生成摘要。 |
| 2 | 🔠 行动选项 | f8a70e57-0cb7-409c-95ab-7df69a737d5f | 开 | - | 开启后在回复末尾提供下一步行动选项，可直接点击推进剧情；不开则需要全手动输入行动。 |
| 3 | 👥 角色存档 | 3ef16a01-56ce-4c5a-ba6c-4310361bbdda | 开 | - | 开启后在回复末尾自动记录与更新登场角色人设，防止跨轮次遗忘；不开则新角色特征可能随对话丢失。 |
| 合计 | 共 3 条 | - | 开 3 / 关 0 | - | - |

### 3.6 🧠 推理选项

- 层级：`🧠 推理选项🔻` … `🧠 推理选项🔺`（order 143 … 162，两条层级条目本身**锁定为开、不出现在面板**）
- 规模：卡片 5 张 ｜ 卡片内条目 13 条 ｜ 独立条目 0 条 ｜ 变量条目 0 条

**卡片：🔧 [推理工具]（多选）** ｜ 卡片头 identifier `5792a718-2557-4368-8c2a-cfb6699aaf4e` ｜ 当前 开 ｜ 条目 **4** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🎭 角色生成 | prompt_1789213990774_30y407j | 开 | - | 开启后在思考中自动推演新登场角色的人设与定位，让角色更立体；不开则新角色出场较随意。 |
| 2 | ☑️ 逻辑校验 | prompt_1789215251350_l1dts86 | 开 | - | 开启后在动笔前快速自查时空、人设与设定连贯性，大幅减少穿帮；不开则容易出现前后矛盾。 |
| 3 | 📚 翻阅世界书 | 6266b09d-6d60-42af-9b73-f250087ce282 | 关 | - | 开启后强制在思考中梳理世界书设定与规则，严格遵循背景；不开则仅依赖模型的自然注意力。 |
| 4 | 📜 腹稿优化(容易雷霆大思考) | 2984cedb-785a-40c8-9812-f97c8049de65 | 开 | - | 开启后在思考中先打草稿并多轮润色，成稿文笔更佳但消耗较多思考字数；不开则直接成稿。 |
| 合计 | 共 4 条 | - | 开 3 / 关 1 | - | - |

**卡片：🔀 [增强随机]（单选）** ｜ 卡片头 identifier `ac5f5d41-9813-48d1-8ff4-1710e321d8b0` ｜ 当前 开 ｜ 条目 **3** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | ❌ 不开 | f7dbbcb1-0633-4108-be77-94da249e079f | 关 | - | （无注释） |
| 2 | 🔮 塔罗牌 | be196908-b8cd-4fc7-ae3a-7ffce64125bb | 开 | - | 开启后每轮随机抽取一张塔罗牌指示运势，注入变数与灵感；不开则完全按常规因果推演。 |
| 3 | ☯️ 易经八卦 | 51fef4ae-d44c-4064-b8cf-9532074b086f | 关 | - | 开启后依据六十四卦推演外在环境与敌势走向，增添吉凶变数；不开则不进行卦象推演。 |
| 合计 | 共 3 条 | - | 开 1 / 关 2 | - | - |

**卡片：🪓 [底部破限]（单选）** ｜ 卡片头 identifier `ade520fa-e390-4139-823d-b050f7a79bd1` ｜ 当前 开 ｜ 条目 **2** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | ❌ 不开 | prompt_1789302116171_1uov3pt | 关 | - | （无注释） |
| 2 | 🐋 DS/GLM底部破限 💤 | c92435fe-0a90-4a3b-8ba4-8b3c6f78aa65 | 关 | 🐋 💤 | 开启后引导模型保持开放专注、不做道德批判，缓解敏感内容拒答；不开则使用模型默认安全策略。 |
| 合计 | 共 2 条 | - | 开 0 / 关 2 | - | - |

**卡片：🤔 [推理格式]（单选）** ｜ 卡片头 identifier `prompt_1789213986821_02eld1m` ｜ 当前 开 ｜ 条目 **2** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | 🐋 DS/GLM强强 💤 | prompt_1789199973145_mdjojp9 | 关 | 🐋 💤 | 开启后为 DeepSeek 与 GLM 模型应用专属思考模板，使思考条理分明；非对应模型请关闭。 |
| 2 | 🐱 Gemini糖糖 | 18d441df-d6b6-4072-bdff-165d7eea0bda | 开 | 🐱 | 开启后为 Gemini 模型应用主持人思维模板，丰富推演细节；非 Gemini 模型请关闭。 |
| 合计 | 共 2 条 | - | 开 1 / 关 1 | - | - |

**卡片：♿ [卡思维链]（单选）** ｜ 卡片头 identifier `7fc82588-17d7-49b3-9653-bda484232ebb` ｜ 当前 开 ｜ 条目 **2** 条

| # | 条目名 | identifier | 当前 | 模型归属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|
| 1 | ❌ 不开 | 45529b50-abff-4121-91b9-9aedb4bda34e | 关 | - | （无注释） |
| 2 | 🐱 Gemini卡思维链 | 6257a576-16bb-4c78-9e15-a421e282eeae | 开 | 🐱 | 开启后注入超限伪标记阻止 Gemini 模型进行冗长原生思考，大幅加快响应；想看完整思考或非 Gemini 请关闭。 |
| 合计 | 共 2 条 | - | 开 1 / 关 1 | - | - |

### 3.7 🤖 模型（全局特殊 tab）

详细清单见第 4 节。此处只记规模：4 张卡片（一个模型一张）/ 12 条条目（含镜像）。

### 3.8 不出 tab 的层级（显式列出）

| 层级 | order 区间 | 为什么不出 tab |
|---|---|---|
| 🪓 神秘咒语 | 9 … 16 | 层级里**唯一的卡片** `🤖 [模型类型]` 被**规则 15（收窄版）** 判掉（它是本层唯一的卡片，且名下 3 条条目**全是模型专属条目**）→ 变成「无卡片无变量」→ 按规则 8 不出 tab；那 3 条仍在「🤖 模型」tab 里（见第 11.5 节） |
| 📌 任务设置 | 17 … 22 | 层级内既没有卡片、也没有变量条目 → 按规则 8 不出 tab（这层的条目会掉出面板，见第 9 节） |
| 系统提示词 | 123 … 134 | 是结构包裹区（名字里同一个箭头符号出现 2 次），整段算结构区，不给用户入口 |
| 交互历史 | 135 … 139 | 是结构包裹区（名字里同一个箭头符号出现 2 次），整段算结构区，不给用户入口 |

- `🪓 神秘咒语` 层级：order 9 … 16，区间内共 6 条普通条目（不含两条锁定层级条目）→ 在「🤖 模型」tab 里能找到 **5 条**，掉出面板 **1 条**（规则 15 判掉的卡片：`🤖 [模型类型]`）。
- `📌 任务设置` 层级：order 17 … 22，区间内共 4 条普通条目（不含两条锁定层级条目）→ 在「🤖 模型」tab 里能找到 **0 条**，掉出面板 **4 条**。
- `🔻系统提示词🔻` … `🔺系统提示词🔺`：结构包裹区（12 条）。
- `🔻交互历史🔻` … `🔺交互历史🔺`：结构包裹区（5 条）。

## 4. 「🤖 模型」tab（全局特殊 tab）

模型类别不硬编码，全部从登记表 `📋 支持模型列表 | model` 的 content 里解析出来（每行以**全角冒号** `：` 分隔）：

| emoji | 解析出的模型名（逐字，规则 6：冒号后那部分） | 来自哪条登记表 |
|---|---|---|
| 🐋 | 表示DeepSeek系列模型 | 📋 支持模型列表 &#124; model |
| 💤 | 表示GLM系列模型 | 📋 支持模型列表 &#124; model |
| 🐱 | 表示Gemini系列模型 | 📋 支持模型列表 &#124; model |
| 🌒 | 表示Kimi系列模型 | 📋 支持模型列表 &#124; model |

> 模型名按规则**逐字保留**（含「表示…系列模型」字样）。面板如果想显示成「DeepSeek」需要用户再拍一次板，见第 10 节。

### 4.1 每个模型的条目清单

**🐋 表示DeepSeek系列模型**（3 条）

| # | 条目名 | identifier | 当前 | 原本在哪张卡片 | 镜像/独有 |
|---|---|---|---|---|---|
| 1 | 🐋 DS/GLM 💤 | prompt_1788967671203_vigbgmo | 关 | 原卡片 🤖 [模型类型]（被规则 15 判掉）→ 现在是 🪓 神秘咒语 层级的裸放条目 | 仅模型 tab |
| 2 | 🐋 DS/GLM底部破限 💤 | c92435fe-0a90-4a3b-8ba4-8b3c6f78aa65 | 关 | 🪓 [底部破限]（在 🧠 推理选项 层级） | 镜像 |
| 3 | 🐋 DS/GLM强强 💤 | prompt_1789199973145_mdjojp9 | 关 | 🤔 [推理格式]（在 🧠 推理选项 层级） | 镜像 |

**💤 表示GLM系列模型**（3 条）

| # | 条目名 | identifier | 当前 | 原本在哪张卡片 | 镜像/独有 |
|---|---|---|---|---|---|
| 1 | 🐋 DS/GLM 💤 | prompt_1788967671203_vigbgmo | 关 | 原卡片 🤖 [模型类型]（被规则 15 判掉）→ 现在是 🪓 神秘咒语 层级的裸放条目 | 仅模型 tab |
| 2 | 🐋 DS/GLM底部破限 💤 | c92435fe-0a90-4a3b-8ba4-8b3c6f78aa65 | 关 | 🪓 [底部破限]（在 🧠 推理选项 层级） | 镜像 |
| 3 | 🐋 DS/GLM强强 💤 | prompt_1789199973145_mdjojp9 | 关 | 🤔 [推理格式]（在 🧠 推理选项 层级） | 镜像 |

**🐱 表示Gemini系列模型**（5 条）

| # | 条目名 | identifier | 当前 | 原本在哪张卡片 | 镜像/独有 |
|---|---|---|---|---|---|
| 1 | 🐱 Gemini随机数 | prompt_1789200146436_lolbt7c | 开 | 原本不在任何卡片里（只在 🪓 神秘咒语 层级裸放） | 仅模型 tab |
| 2 | 🐱 Gemini抗输入审 | prompt_1789339450008_wci4d02 | 关 | 原本不在任何卡片里（只在 🪓 神秘咒语 层级裸放） | 仅模型 tab |
| 3 | 🐱 Gemini | a03f3f0b-0c4d-43a4-aab9-fc34910a7121 | 开 | 原卡片 🤖 [模型类型]（被规则 15 判掉）→ 现在是 🪓 神秘咒语 层级的裸放条目 | 仅模型 tab |
| 4 | 🐱 Gemini糖糖 | 18d441df-d6b6-4072-bdff-165d7eea0bda | 开 | 🤔 [推理格式]（在 🧠 推理选项 层级） | 镜像 |
| 5 | 🐱 Gemini卡思维链 | 6257a576-16bb-4c78-9e15-a421e282eeae | 开 | ♿ [卡思维链]（在 🧠 推理选项 层级） | 镜像 |

**🌒 表示Kimi系列模型**（1 条）

| # | 条目名 | identifier | 当前 | 原本在哪张卡片 | 镜像/独有 |
|---|---|---|---|---|---|
| 1 | 🌒 KIMI(占位) | 94df9413-488a-4912-87e3-ec6bd2d345e8 | 关 | 原卡片 🤖 [模型类型]（被规则 15 判掉）→ 现在是 🪓 神秘咒语 层级的裸放条目 | 仅模型 tab |

### 4.2 多模型共用条目（显式列出）

| 条目名 | identifier | 归属集合 | 原本在哪张卡片 | 当前 |
|---|---|---|---|---|
| 🐋 DS/GLM 💤 | prompt_1788967671203_vigbgmo | 🐋 + 💤 | 原卡片 🤖 [模型类型]（被规则 15 判掉） | 关 |
| 🐋 DS/GLM底部破限 💤 | c92435fe-0a90-4a3b-8ba4-8b3c6f78aa65 | 🐋 + 💤 | 🪓 [底部破限] | 关 |
| 🐋 DS/GLM强强 💤 | prompt_1789199973145_mdjojp9 | 🐋 + 💤 | 🤔 [推理格式] | 关 |

- 共 **3 条**多模型共用条目，全部同时属于 🐋 和 💤。
- 它们会在「🤖 模型」tab 里**各出现两次**（🐋 卡片一次、💤 卡片一次）。
- 模型闸门（规则 11）：面板拿 `models` 数组做判断；多归属条目「任一命中即开放」还是「必须全部命中」，见第 10 节待拍板。

### 4.3 镜像关系汇总

| 条目名 | 归属 | 原位置（层级 / 卡片） | 模型 tab 里的身份 | 原卡片里是否标注模型专属 |
|---|---|---|---|---|
| 🐱 Gemini随机数 | 🐱 | 🪓 神秘咒语 层级 / （无卡片） | 仅存在于模型 tab（originCard=null） | 无原卡片可标注 |
| 🐱 Gemini抗输入审 | 🐱 | 🪓 神秘咒语 层级 / （无卡片） | 仅存在于模型 tab（originCard=null） | 无原卡片可标注 |
| 🐋 DS/GLM 💤 | 🐋 💤 | 🪓 神秘咒语 层级 / （原卡片 🤖 [模型类型] 已被规则 15 判掉，现在是层级里的裸放条目） | 仅存在于模型 tab（originCard=null，原卡片被规则 15 判掉） | 无原卡片可标注 |
| 🐱 Gemini | 🐱 | 🪓 神秘咒语 层级 / （原卡片 🤖 [模型类型] 已被规则 15 判掉，现在是层级里的裸放条目） | 仅存在于模型 tab（originCard=null，原卡片被规则 15 判掉） | 无原卡片可标注 |
| 🌒 KIMI(占位) | 🌒 | 🪓 神秘咒语 层级 / （原卡片 🤖 [模型类型] 已被规则 15 判掉，现在是层级里的裸放条目） | 仅存在于模型 tab（originCard=null，原卡片被规则 15 判掉） | 无原卡片可标注 |
| 🐋 DS/GLM底部破限 💤 | 🐋 💤 | 🧠 推理选项 层级 / 🪓 [底部破限] | 镜像（isMirror=true） | 是（modelExclusive=true） |
| 🐋 DS/GLM强强 💤 | 🐋 💤 | 🧠 推理选项 层级 / 🤔 [推理格式] | 镜像（isMirror=true） | 是（modelExclusive=true） |
| 🐱 Gemini糖糖 | 🐱 | 🧠 推理选项 层级 / 🤔 [推理格式] | 镜像（isMirror=true） | 是（modelExclusive=true） |
| 🐱 Gemini卡思维链 | 🐱 | 🧠 推理选项 层级 / ♿ [卡思维链] | 镜像（isMirror=true） | 是（modelExclusive=true） |

- 唯一模型条目 **9 条**；出现次数 = 原生位置 9 次 + 模型 tab 12 次 = **21 次**（含镜像）。
- 其中 **6 次**是「还挂在层级卡片里的条目镜像到模型 tab」（`isMirror=true`），**6 次**是「已经没有原卡片、只出现在模型 tab」（`originCard=null`）。
- 规则 15 的直接后果：`🤖 [模型类型]` 这 1 张卡片被判掉，名下共 3 条的 `originCard` 从卡片名变成 `null` → **镜像 10 次 → 6 次**，**仅模型 tab 2 次 → 6 次**（模型 tab 内的总出现次数 12 次不变）。
- `🐱 Gemini随机数` / `🐱 Gemini抗输入审` 这两条本来就在 `🪓 神秘咒语` 层级里裸放（一直不在任何卡片内），与本轮改动无关。

## 5. 「🧩 设置变量」tab

三条变量条目（名字末尾 ` | var`），共解析出 **5 个变量**，合并成 **3 张变量卡片**。

变量名与当前值只从「条目自己的 content」里读，且**只保留值为纯数字的** `setvar::名::值` 片段（值为空的一律不算）。

| 变量条目 | 变量名 | 当前值 | 判成什么卡片 | 依据 |
|---|---|---|---|---|
| 🧩 推理预算 &#124; var | cot_budget | 6000 | 数字卡（单个变量） | 没有配对的 _min/_max，单独一张 |
| 🧩 正文字数 &#124; var | content_word_count_min | 1500 | 范围卡（大小正常） | content_word_count 这一对 _min/_max 合并 |
| 🧩 正文字数 &#124; var | content_word_count_max | 3000 | 范围卡（同一个 base=content_word_count） | 与上一行同属一张卡的两个数字框 |
| 🧩 插图数量 &#124; var | image_count_min | 4 | 范围卡（大小正常） | image_count 这一对 _min/_max 合并 |
| 🧩 插图数量 &#124; var | image_count_max | 8 | 范围卡（同一个 base=image_count） | 与上一行同属一张卡的两个数字框 |

**配对明细**

- `🧩 正文字数 | var` → `content_word_count_min=1500` + `content_word_count_max=3000` → 去掉后缀后同名（`content_word_count`）→ **1 张范围卡**（2 个数字框）。
- `🧩 插图数量 | var` → `image_count_min=4` + `image_count_max=8` → 去掉后缀后同名（`image_count`）→ **1 张范围卡**（2 个数字框）。
- `🧩 推理预算 | var` → 只有 `cot_budget=6000`，没有 _min/_max 配对 → **1 张数字卡**（1 个数字框）。
- 合计：**2 组被判成范围卡片**（正文字数、插图数量），1 张数字卡片（推理预算），共 5 个变量。

**被「纯数字」这道闸挡掉的片段**（这条规则很关键，实测数据如下）：

| 条目（没有  &#124; var 后缀） | setvar 片段数 | 值为空 | 纯数字 | 非数字（标签/文本） |
|---|---|---|---|---|
| 🧩 清空变量 | 32 | 32 | 0 | 0 |
| 🧩 标签格式 | 22 | 0 | 0 | 22 |

- 这两条是「开关型」条目：🧩 清空变量 的 32 处 setvar 全部用来**清空**变量，值是空串；🧩 标签格式 的值是 `<think>` 这类标签文本。它们本来就没有 ` | var` 后缀、不会被当变量，即便误读也会被「值必须纯数字」再挡一道（实测纯数字 0 处）。

## 6. 结构区清单

结构区 = 面板上不出现、只统计条数的部分。本预设结构区共 **19 条**：不在任何层级内且不是 marker 的裸条目 2 条 + 两个包裹区 17 条。

### 6.1 不在任何层级内的裸条目（2 条）

| # | order | 条目名 | identifier | 当前 | content 长度 | 说明（截断显示，完整见 content） |
|---|---|---|---|---|---|---|
| 1 | 0 | ‼️免责声明/开源许可‼️(不开) | prompt_1788362726454_a5al6o6 | 关 | 1901 | 卡密预设（Kami Preset）免责声明与开源许可  **版本：** v0.9 **作者：** kamisama **适用平台：** SillyTavern（版本1.18以上） … |
| 2 | 142 | 🗂️ 最近用户输入 | prompt_1789302232751_2v7omt6 | 开 | 80 | （无注释） |

### 6.2 结构包裹区（2 段 / 17 条）

**系统提示词**：`🔻系统提示词🔻` … `🔺系统提示词🔺`，order 123 … 134，共 12 条：

- 🔻系统提示词🔻（普通条目，开）
- 🍺 World Info (before)（marker:true，开）
- ↓用户角色↓（普通条目，开）
- 🍺 Persona Description（marker:true，开）
- ↑用户角色↑（普通条目，开）
- ↓AI角色↓（普通条目，开）
- 🍺 Char Description（marker:true，开）
- 🍺 Char Personality（marker:true，开）
- ↑AI角色↑（普通条目，开）
- 🍺 Scenario（marker:true，开）
- 🍺 World Info (after)（marker:true，开）
- 🔺系统提示词🔺（普通条目，开）

**交互历史**：`🔻交互历史🔻` … `🔺交互历史🔺`，order 135 … 139，共 5 条：

- 🔻交互历史🔻（普通条目，开）
- Chat Examples（marker:true，开）
- Agent Task（marker:true，开）
- Chat History（marker:true，开）
- 🔺交互历史🔺（普通条目，开）

### 6.3 系统 marker 条目（11 条，附带说明）

这 11 条是 SillyTavern 的系统占位（`marker: true`）。其中 9 条落在包裹区内、已计入 6.2；剩下 2 条 `Agent System Prompt` / `Agent Results` 不在任何区间内，按规则 3「是 marker 的条目不算结构区」单独列出——它们本来也没有正文内容。

| 条目名 | identifier | enabled | 有 content 字段吗 | content 长度 |
|---|---|---|---|---|
| 🍺 World Info (before) | worldInfoBefore | 开 | 没有 | 0 |
| 🍺 Persona Description | personaDescription | 开 | 没有 | 0 |
| 🍺 Char Description | charDescription | 开 | 没有 | 0 |
| 🍺 Char Personality | charPersonality | 开 | 没有 | 0 |
| 🍺 Scenario | scenario | 开 | 没有 | 0 |
| 🍺 World Info (after) | worldInfoAfter | 开 | 没有 | 0 |
| Chat Examples | dialogueExamples | 开 | 没有 | 0 |
| Agent Task | agentTask | 开 | 有 | 0 |
| Chat History | chatHistory | 开 | 没有 | 0 |
| Agent System Prompt | agentSystemPrompt | 开 | 有 | 0 |
| Agent Results | agentResults | 开 | 有 | 0 |

## 7. 锁定条目清单（层级条目：锁定为开、不可关、不出现在面板）

共 **16 条**层级条目（8 个层级 × 开/闭各 1 条）。解析器**绝不修改** `enabled`，只报告。

| # | order | 层级 | 开/闭 | 条目名 | identifier | 短号（后 6 位） | 当前 enabled |
|---|---|---|---|---|---|---|---|
| 1 | 2 | 🧩 设置变量 | 层级开 | 🧩 设置变量🔻 | 89e26f53-949c-4d1f-9f3a-ae390b4b89d7 | 4b89d7 | 开 |
| 2 | 8 | 🧩 设置变量 | 层级闭 | 🧩 设置变量🔺 | prompt_1788362771382_dc5i4kj | c5i4kj | 开 |
| 3 | 9 | 🪓 神秘咒语 | 层级开 | 🪓 神秘咒语🔻 | prompt_1789200359809_g3ptbqx | 3ptbqx | 开 |
| 4 | 16 | 🪓 神秘咒语 | 层级闭 | 🪓 神秘咒语🔺 | prompt_1789200368213_tyr3fjy | yr3fjy | 开 |
| 5 | 17 | 📌 任务设置 | 层级开 | 📌 任务设置🔻 | prompt_1788364117730_a4onlpt | 4onlpt | 开 |
| 6 | 22 | 📌 任务设置 | 层级闭 | 📌 任务设置🔺 | prompt_1788364124117_zrv9vrr | rv9vrr | 开 |
| 7 | 23 | 📖 创作要求 | 层级开 | 📖 创作要求🔻 | prompt_1788265221945_4dy1fw2 | dy1fw2 | 开 |
| 8 | 64 | 📖 创作要求 | 层级闭 | 📖 创作要求🔺 | prompt_1788265232728_jszh9pr | szh9pr | 开 |
| 9 | 65 | 😀 角色塑造 | 层级开 | 😀 角色塑造🔻 | prompt_1788267836372_bss4xtu | ss4xtu | 开 |
| 10 | 75 | 😀 角色塑造 | 层级闭 | 😀 角色塑造🔺 | prompt_1788267848284_wj5slql | j5slql | 开 |
| 11 | 76 | 📝 写作指导 | 层级开 | 📝 写作指导🔻 | prompt_1789201562750_tmqkrui | mqkrui | 开 |
| 12 | 111 | 📝 写作指导 | 层级闭 | 📝 写作指导🔺 | prompt_1789201569996_omlzfic | mlzfic | 开 |
| 13 | 112 | 🧪 额外任务 | 层级开 | 🧪 额外任务🔻 | 08c70a9d-7f10-47da-ba03-38df1298fb11 | 98fb11 | 开 |
| 14 | 122 | 🧪 额外任务 | 层级闭 | 🧪 额外任务🔺 | 26f0f7e1-c954-4f54-9b03-1918b87ba35d | 7ba35d | 开 |
| 15 | 143 | 🧠 推理选项 | 层级开 | 🧠 推理选项🔻 | prompt_1789196189509_9okdaj0 | okdaj0 | 开 |
| 16 | 162 | 🧠 推理选项 | 层级闭 | 🧠 推理选项🔺 | prompt_1789196194740_2b1svo4 | b1svo4 | 开 |

**没有一条层级条目是关掉的**（`enabled: false` 命中 0 条）——所以面板不需要显示「层级被关掉」的告警行。

## 8. 统计

| 指标 | 数值 | 口径 |
| --- | --- | --- |
| tab 数 | **7**（层级 tab 6 + 全局「🤖 模型」tab 1）｜完全不加规则 15 时是 **8** | 规则 8 + 规则 9（叠加规则 15 收窄版） |
| 卡片数 | **25**（层级卡片 21 + 模型卡片 4）｜完全不加规则 15 时层级卡片是 22 张 | 规则 4 / 规则 9（叠加规则 15） |
| 零条目卡片降级 | 0 张（没有卡片头因为「零条目」被降级） | 规则 4 的「零条目的卡片不算卡片」（原有） |
| 模型专属卡片降级 | **1 张卡片头**（1 张卡片 / 名下 3 条条目） | **规则 15（收窄版）**，见第 11 节 |
| ↳ 被收窄放过的卡片 | **1 张**：`🤔 [推理格式]`（在 `🧠 推理选项` 层级，该层共 5 张卡片） —— 名下条目也全是模型专属，但**不是该层唯一的卡片** → 保持卡片身份 | 规则 15（收窄版）新增的第二道条件 |
| 可开关条目数 | **99** 条（去重）｜完全不加规则 15 时是 99 条 | 出现在 tab 里、且是可开关的内容条目（含变量条目 3）|
| ↳ 其中的降级卡片头 | 0 条（（无））：收窄后不再出现「被降级的卡片头又落在某个层级 tab 里」这种情况 | 规则 15（收窄版） |
| 卡片头 | 21 条｜完全不加规则 15 时是 22 条（少了 1 张被降级的卡片头） | 在面板上显示为卡片标题，不算「可开关条目」 |
| 面板条目合计 | 120 条（=99 + 21）｜完全不加规则 15 时是 121 条 | 能在面板上找到的条目（去重） |
| 模型条目数 | 唯一 **9** 条；模型 tab 内出现 **12** 次；连同各自的原生位置合计 **21** 次（含镜像） | 规则 7 / 10（规则 15 不改这些数字） |
| ↳ 模型 tab 内出现 | 12 次（其中镜像 6 次、仅模型 tab 6 次）｜完全不加规则 15 时是镜像 10 / 仅模型 2 | 规则 9 / 10（叠加规则 15） |
| ↳ 模型 tab 里每张卡片 | 🐋 表示DeepSeek系列模型=3 条 / 💤 表示GLM系列模型=3 条 / 🐱 表示Gemini系列模型=5 条 / 🌒 表示Kimi系列模型=1 条 | 规则 9（规则 15 前后都是这些数字） |
| ↳ 多模型共用 | 3 条 | 规则 7 |
| 变量 | 3 条变量条目 → 5 个变量 → 2 张范围卡 + 1 张数字卡 | 规则 5 |
| 结构区 | 2 条裸条目 + 17 条包裹区 = **19 条** | 规则 3 |
| 系统 marker | 2 条（包裹区外的）+ 9 条包裹区内的 | 规则 3 |
| 锁定层级条目 | 16 条（其中关着的 0 条） | 规则 12 |
| 注释覆盖率 | 面板条目 120 条中 54 条有注释 = **45.0%**；只算可开关内容条目则 33/99 = **33.3%** | 规则 13（注释缺失不是错误） |
| 面板里当前「关」着的可开关条目 | 60 / 99 条 | 只统计，不修改（规则 14） |
| 面板上找不到的条目 | **5 条** | 见第 9 节 |

> 注释覆盖率的两个数字与「完全不加规则 15」相比只差一处（面板 121 → 120，有注释 55 → 54）：规则 15 让面板少了 1 条**带注释的卡片头**（`🤖 [模型类型] 选一` 脱离面板）、分母同时少 1。**没有任何条目的注释被改动或删除。**（收窄只影响另外那一张卡片：`🤔 [推理格式]` 的 2 条条目身份不变，所以它不再影响覆盖率。）

**有注释的条目（共 54 条 = 内容条目 33 条 + 卡片头 21 条）**

- 内容条目（33 条）：🧩 清空变量、🧩 标签格式、🧩 推理预算 | var、🧩 正文字数 | var、🧩 插图数量 | var、🐱 Gemini随机数、🐱 Gemini抗输入审、💕 温柔色色、💕 多轮次色色、⚙️ 尾部仿出戏、🚫 防全知、🚫 防绝望、🚫 防OOC、🚫 防发情、🚫 防冰冷、🚫 防神化、🚫 防讨好、🚫 防说教、🎥 HTML美化、🎨 文生图、🧱 摘要(小总结)、🔠 行动选项、👥 角色存档、🎭 角色生成、☑️ 逻辑校验、📚 翻阅世界书、📜 腹稿优化(容易雷霆大思考)、🔮 塔罗牌、☯️ 易经八卦、🐋 DS/GLM底部破限 💤、🐋 DS/GLM强强 💤、🐱 Gemini糖糖、🐱 Gemini卡思维链
- 卡片头（21 条）：🗣️ [语言文字] 选一、🏃🏻‍♂️ [剧情推进] 选一、👁️ [叙事视角] 选一、🎮 [创作权限] 选一、🔊 [转述程度] 选一、⚔️ [难度设定] 选一、🎯 [叙事焦点] 选一、💕 [色色选项] 任选、⚙️ [杂项要求] 任选、🚫 [各种防止] 任选、✒️ [文风优化] 任选、🖊️ [写作文风] 选一、🔞 [NSFW文风] 选一、🔝 [头部任务] 任选、🔛 [正文任务] 任选、🔚 [尾部任务] 任选、🔧 [推理工具] 任选、🔀 [增强随机] 选一、🪓 [底部破限] 选一、🤔 [推理格式] 选一、♿ [卡思维链] 选一
- 其余条目内容里**没有** `{{// }}` 注释 —— 按规则 13，这不是错误。

## 9. 报告正文里必须回答的问题：有没有条目在面板上找不到？

问题：**按这套规则解析这份预设，有没有任何一条真实内容会在面板上找不到（既不在任何 tab 里、也不是结构区/锁定条目）？**

答案：**有，5 条。** 分两组：

**第一组（规则 4/8 原有，4 条）**：全部来自 `📌 任务设置` 层级（order 17 … 22，4 条内容条目在 order 18、19、20、21）：层级里既没有 ` 选一` / ` 任选` 卡片、也没有 ` | var` 变量条目，按规则 8「有卡片或有变量才出 tab」它不出 tab；而这 4 条确实在层级内（所以不算结构区）、名字里没有模型 emoji（所以不进「🤖 模型」tab）、也不是 marker。**这是上一轮就已存在、用户已确认接受的行为。**

**第二组（规则 15 收窄后仍存在，1 条）**：`🤖 [模型类型] 选一` —— 它是 `🪓 神秘咒语` 层级里那张被规则 15 判掉的卡片的**卡片头**。**收窄没有让它回到 4 条**：因为那张卡片是 `🪓 神秘咒语` 层级里**唯一的卡片**（该层共 1 张），收窄后的规则照样命中；卡片被判掉后它降级成普通条目，而该层级又不出 tab，于是失去面板入口。它的 content 只有一条 `{{// }}` 注释（60 字符）：`不同模型需要不同的适配方法，条目名有一种emoji的即为该模型专属，选择某一模型时必须关闭其他模型专属条目。`，没有任何正文/指令文本。**这一条仍需用户单独拍板**（见第 10 节第 12 条与第 11.7 节）。

| # | order | 条目名 | identifier | 短号 | 当前 enabled | content 长度 | 所属 | 说明（第一个 {{// }}） |
|---|---|---|---|---|---|---|---|---|
| 1 | 12 | 🤖 [模型类型] 选一 | prompt_1789213815769_ks7pgcc | s7pgcc | 开 | 60 | 规则 15 降级的卡片头 | 不同模型需要不同的适配方法，条目名有一种emoji的即为该模型专属，选择某一模型时必须关闭其他模型专属条目。 |
| 2 | 18 | 📌 模型任务 | prompt_1788364268110_uiz8qyi | iz8qyi | 开 | 273 | 📌 任务设置 层级 | （无注释） |
| 3 | 19 | 📌 身份权限 | prompt_1788364241843_29k9zpo | 9k9zpo | 开 | 698 | 📌 任务设置 层级 | （无注释） |
| 4 | 20 | 📌 输入解析 | prompt_1788966454790_67nbvel | 7nbvel | 开 | 320 | 📌 任务设置 层级 | （无注释） |
| 5 | 21 | 📌 输出规范 | prompt_1788364276043_04lx6bt | 4lx6bt | 开 | 3141 | 📌 任务设置 层级 | （无注释） |

这 5 条里：4 条是**实打实有正文的条目**（content 长度见上表，全部来自 `📌 任务设置`，处理方式见第 10 节第 1 条），1 条是**注释型卡片头**（规则 15 的副产物，见第 10 节第 12 条）。

**其余全部有归属**，逐类复核如下：

| 类别 | 条数 | 说明 |
|---|---|---|
| 面板 tab 里的条目 | 120 | 含卡片内条目、独立条目、变量条目、卡片头 |
| 结构区裸条目 | 2 | ‼️免责声明/开源许可‼️(不开)、🗂️ 最近用户输入 |
| 结构包裹区 | 17 | 🔻系统提示词🔻…🔺系统提示词🔺 12 条 + 🔻交互历史🔻…🔺交互历史🔺 5 条 |
| 锁定层级条目 | 16 | 8 个层级的开/闭各 1 条 |
| 系统 marker（包裹区外） | 2 | Agent System Prompt、Agent Results（无正文内容） |
| 模型登记表条目 | 1 | 📋 支持模型列表 &#124; model（是 4 个模型的表源，按规则 6 单列，见第 10 节第 3 条） |
| 找不到的条目 | 5 | 上表 5 条（4 条 📌 任务设置 + 1 条规则 15 判掉的卡片头） |

> 对账：order 里共 163 条，上面的分类互不重叠，合计 163 条 = 面板 120 + 结构 2 + 包裹 17 + 锁定 16 + marker 2 + 登记表 1 + 找不到 5。模型 tab 里的条目是镜像，不重复计入面板去重数。

## 10. 拿不准 / 需要拍板

1. **`📌 任务设置` 层级的 4 条条目掉出面板（最重要）**：`📌 模型任务` / `📌 身份权限` / `📌 输入解析` / `📌 输出规范`。规则 8 的直接后果是「无卡片无变量 → 不出 tab」。可选：(a) 在该层级第一行加一条卡片头（如 `📌 [任务设置] 任选`）；(b) 把 4 条当"常开基础设施"并入结构区；(c) 给面板加一条"其它层级"兜底视图。**需要用户选一个。**
2. **`🗂️ 最近用户输入`（order 142）落在结构区**：它不在任何 `X🔻…X🔺` 区间内，按规则 3 就是结构区，但它是 80 字符的实内容，而且没有 `{{// }}` 注释。面板上不会有入口，需确认这是否符合预期。
3. **模型登记表 `📋 支持模型列表 | model` 本身不在任何层级内**：规则 3 字面上会说它「不在任何层级区间内 → 结构区」，但规则 6 又说它是模型登记表。本解析器让规则 5/6 优先，把它标成 `model-registry`（不算结构区、也不是任何 tab 的条目）。建议面板把它当作「🤖 模型」tab 的表头/图例展示（不可开关）。**需确认这个优先顺序。**
4. **`🪓 神秘咒语` 层级：用户已裁定 —— 不出 tab。** 该层级里唯一的卡片 `🤖 [模型类型]` 名下 3 条条目**全是模型专属条目**（🐋 DS/GLM 💤、🐱 Gemini、🌒 KIMI(占位)），按**规则 15（收窄版：必须是该层唯一的卡片）**这张卡片不算卡片 → 该层级变成「无卡片无变量」→ 按规则 8 不出 tab。那 3 条仍然出现在「🤖 模型」tab 里（逐条核对见第 11.5 / 11.6 节），**没有任何条目因此从面板消失**。副作用见下面第 12 条。
5. **模型名逐字保留**：规则 6 说取全角冒号后那部分，于是模型名是 `表示DeepSeek系列模型` / `表示GLM系列模型` / `表示Gemini系列模型` / `表示Kimi系列模型`。面板上如果只想显示「DeepSeek」，需要再给一条规则（本次没有自行推断）。
6. **模型闸门的多归属语义**：3 条同时属于 🐋 和 💤。面板实现「非当前模型的条目开关禁用」时，是「命中任意一个当前模型就开放」还是「必须命中全部」？解析器已经把归属集合（`models` 数组）带出来了，语义由面板定。
7. **重名条目**：`❌ 不开` 出现 4 次（分别在 🔞 [NSFW文风]、🔀 [增强随机]、🪓 [底部破限]、♿ [卡思维链] 四张卡片里），`🌒 KIMI(占位)` / `占位(暂时只有Gemini的显式思维链)` 是占位条目。主键是 identifier 所以不冲突，但面板上要不要加卡片前缀、要不要隐藏占位条目，需要拍板。
8. **注释覆盖率很低**：可开关内容条目 99 条里只有 33 条自带 `{{// … }}` 注释（33.3%）。面板想给每条目一行说明的话，需要另做一份「条目说明补全表」（本次没做，规则 13 明确说注释缺失不是错误）。
9. **order.enabled 与 prompts.enabled 有 1 条不一致**：`🐱 Gemini抗输入审`（order=关，prompts=开）。解析器统一取 order 里的值（SillyTavern 也以 order 为准），面板写回时要注意两边都要同步。
10. **A2 名字已定稿**：`📋 支持模型列表 | model` 是用户定稿名，不再是占位；emoji 📋（U+1F4CB）已确认在预设里未被占用。若之后改名，只需改 `preset-migrate.mjs` 里的 `NEW_NAME_MODEL` 常量。
11. **已裁定（本轮落地）：规则 15 收窄成「只作用于层级里唯一的卡片」。** 上一版只按「名下条目全是模型专属」判定，一次命中两张：`🤖 [模型类型]`（`🪓 神秘咒语` 层级里唯一的卡片 → 命中是对的，该层不出 tab）与 `🤔 [推理格式]`（`🧠 推理选项` 层级里 5 张卡片中的一张 → **误伤**：它从卡片头掉成普通开关、名下 2 条变成散条目，上一位实现者还临时造了一张叫「独立条目」的假卡片装它们）。收窄后：`🤔 [推理格式]` 恢复成正常卡片（`🧠 推理选项` tab 恢复成 **5 张卡片 / 0 条独立条目**），面板里那张「独立条目」假卡片已一并删除。
12. **被规则 15 判掉的卡片头 `🤖 [模型类型] 选一` 仍然失去面板入口**（`🪓 神秘咒语` 不出 tab，它又没有模型 emoji）：它的 content 只有一条 `{{// }}` 注释（60 字符）、没有正文，所以「面板上找不到的条目」是 5（= 4 条 📌 任务设置 + 这 1 条），**收窄没有把它变回 4 条**（它所在的层级里只有它这一张卡片）。需要确认：(a) 接受（按"注释不算内容"处理）；(b) 把它并入结构区；(c) 在「🤖 模型」tab 里当图例/表头展示（它的注释正好在解释"模型专属"的约定，很可能本来就该当图例）；(d) 给 `🪓 神秘咒语` 层级补一张非模型专属的卡片头（那样该层会重新出 tab，卡片头也就有了面板入口）。**需要用户选一个。**

## 11. 规则 15（收窄版）· 模型专属卡片降级 —— 适用范围与核对结果

### 11.0 规则编号与一句话表述

> **规则 15（收窄版）：一张卡片，如果它 ① 是它所在层级里「唯一的卡片」，而且 ② 名下的条目「全部」都是模型专属条目（每条的模型归属集合都非空），那么这张卡片不算卡片。**

- **收窄的由来（本轮唯一改动）**：旧版只有条件 ②，一次命中了**两张**卡片 —— `🤖 [模型类型]`（`🪓 神秘咒语` 层级里唯一的卡片，命中是对的）与 `🤔 [推理格式]`（`🧠 推理选项` 层级 5 张卡片中的一张，属于**误伤**）。用户实测后指出：「[推理格式] 不应该是卡片层级吗？为什么会有独立条目作为其卡片层级，而本应该是卡片层级的[推理格式]却变成了其中的一个开关？」→ 本轮把规则收窄成「只看层级里**唯一的**那张卡片」。
- 编号说明：本报告沿用的「规则 N」是**需求规则**编号（规则 3 结构区/marker、规则 4 卡片组装与「零条目的卡片不算卡片」、规则 5 变量、规则 6 模型登记表、规则 7 模型归属、规则 8「有卡片或有变量才出 tab」、规则 9 全局「🤖 模型」tab、规则 10 镜像、规则 11 模型闸门、规则 12 锁定层级条目、规则 13 注释、规则 14 只统计不修改）。规则 15 实现在解析器的步骤 6.5（`test/harness/preset-parse.mjs`），原有编号不动。
- 判定条件只看「卡片是不是本层唯一的卡片」+「条目名字里的模型 emoji 归属集合是否非空」，不看 `enabled`、不看 content、不看注释放了什么。
- 判定粒度是**卡片**：只要名下有一条条目**没有**模型归属，这张卡片照旧保留（全量扫描见 11.1）。
- 生效范围只限**层级内的卡片**：包裹区不是层级；「零条目卡片」在步骤 4 就已经降级，不会走到本条。
- 被判定后：卡片头降级成该层级的普通条目（`cardDemoted=true`，`cardDemoteReason="model-exclusive-items"`），名下条目变成该层级的**裸放条目**（`card=null`，`originCard=null`），卡片从层级卡片列表里移除。**收窄后「唯一的卡片」被拿掉，该层级必然变成「无卡片无变量」→ 按规则 8 不出 tab**（不会再出现「卡片被降级、层级还出 tab」的情况）。
- **不变量**：不删任何条目、不改任何 `enabled` / content / name、不改层级内条目顺序。模型专属条目照旧按名字里的 emoji 收进「🤖 模型」tab（规则 7/9/10 完全不受影响）。

### 11.1 全量扫描：逐层级逐卡片

下表是规则 15 的全部判定输入（22 张层级卡片，逐张列出）。第 5 列「该层卡片数」就是收窄后新增的那道闸：**只有它等于 1 的卡片才可能被判掉**。

| # | 层级 | 卡片 | 该层卡片数 | 卡片内条目 | 其中模型专属 | 非模型条目 | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 🪓 神秘咒语 | 🤖 [模型类型] | 1 | 3 | 3 | **0 条** | **判掉（不算卡片）** |
| 2 | 📖 创作要求 | 🗣️ [语言文字] | 9 | 4 | 0 | 4 条 | 保留 |
| 3 | 📖 创作要求 | 🏃🏻‍♂️ [剧情推进] | 9 | 3 | 0 | 3 条 | 保留 |
| 4 | 📖 创作要求 | 👁️ [叙事视角] | 9 | 4 | 0 | 4 条 | 保留 |
| 5 | 📖 创作要求 | 🎮 [创作权限] | 9 | 3 | 0 | 3 条 | 保留 |
| 6 | 📖 创作要求 | 🔊 [转述程度] | 9 | 3 | 0 | 3 条 | 保留 |
| 7 | 📖 创作要求 | ⚔️ [难度设定] | 9 | 6 | 0 | 6 条 | 保留 |
| 8 | 📖 创作要求 | 🎯 [叙事焦点] | 9 | 4 | 0 | 4 条 | 保留 |
| 9 | 📖 创作要求 | 💕 [色色选项] | 9 | 2 | 0 | 2 条 | 保留 |
| 10 | 📖 创作要求 | ⚙️ [杂项要求] | 9 | 2 | 0 | 2 条 | 保留 |
| 11 | 😀 角色塑造 | 🚫 [各种防止] | 1 | 8 | 0 | 8 条 | 保留 |
| 12 | 📝 写作指导 | ✒️ [文风优化] | 3 | 3 | 0 | 3 条 | 保留 |
| 13 | 📝 写作指导 | 🖊️ [写作文风] | 3 | 23 | 0 | 23 条 | 保留 |
| 14 | 📝 写作指导 | 🔞 [NSFW文风] | 3 | 5 | 0 | 5 条 | 保留 |
| 15 | 🧪 额外任务 | 🔝 [头部任务] | 3 | 1 | 0 | 1 条 | 保留 |
| 16 | 🧪 额外任务 | 🔛 [正文任务] | 3 | 2 | 0 | 2 条 | 保留 |
| 17 | 🧪 额外任务 | 🔚 [尾部任务] | 3 | 3 | 0 | 3 条 | 保留 |
| 18 | 🧠 推理选项 | 🔧 [推理工具] | 5 | 4 | 0 | 4 条 | 保留 |
| 19 | 🧠 推理选项 | 🔀 [增强随机] | 5 | 3 | 0 | 3 条 | 保留 |
| 20 | 🧠 推理选项 | 🪓 [底部破限] | 5 | 2 | 1 | 1 条 | 保留 |
| 21 | 🧠 推理选项 | 🤔 [推理格式] | 5 | 2 | 2 | **0 条** | **保留（不是本层唯一的卡片）** |
| 22 | 🧠 推理选项 | ♿ [卡思维链] | 5 | 2 | 1 | 1 条 | 保留 |

- 判定分布：**判掉 1 张** `🤖 [模型类型]` @ 🪓 神秘咒语；**名下条目全是模型专属、但不是本层唯一卡片 → 收窄后保留 1 张** `🤔 [推理格式]` @ 🧠 推理选项；其余 20 张照旧保留。
- 旧版（未收窄）会判掉 **2 张**（= 上面两类之和），多出来的那 1 张就是本轮修掉的误伤。
- 部分命中（卡片里既有模型条目、也有普通条目）的卡片按规则 15 一律**保留**，逐张明细：
  - `🧠 推理选项` / `🪓 [底部破限]`：2 条里 1 条模型专属（🐋 DS/GLM底部破限 💤），另有 1 条无模型归属（❌ 不开）→ **保留卡片**（卡片头照旧出 tab，模型条目同时镜像进「🤖 模型」tab）。
  - `🧠 推理选项` / `♿ [卡思维链]`：2 条里 1 条模型专属（🐱 Gemini卡思维链），另有 1 条无模型归属（❌ 不开）→ **保留卡片**（卡片头照旧出 tab，模型条目同时镜像进「🤖 模型」tab）。

### 11.2 被规则 15 判掉的卡片（1 张）

| 卡片 | 层级 | order 区间 | 模式 | 该层卡片数 | 卡片头 identifier | 卡片头 enabled | 名下条目（模型归属） |
|---|---|---|---|---|---|---|---|
| 🤖 [模型类型] | 🪓 神秘咒语 | 9 … 16 | 单选 | 1 | prompt_1789213815769_ks7pgcc | 开 | 🐋 DS/GLM 💤 [🐋 💤]；🐱 Gemini [🐱]；🌒 KIMI(占位) [🌒] |

### 11.2b 被收窄放过的那张卡片（1 张）—— 本轮修掉的误伤

| 卡片 | 层级 | 该层卡片数 | 同层的其它卡片 | 名下条目（模型归属） | 收窄后的身份 |
|---|---|---|---|---|---|
| 🤔 [推理格式] | 🧠 推理选项 | 5 | 🔧 [推理工具] / 🔀 [增强随机] / 🪓 [底部破限] / ♿ [卡思维链] | 🐋 DS/GLM强强 💤 [🐋 💤]；🐱 Gemini糖糖 [🐱] | **正常卡片**（卡片头 + 2 条卡片内条目） |

- `🤔 [推理格式]` 名下 2 条条目**全是模型专属**，但它所在层级有 5 张卡片 → 不是「唯一的卡片」→ 规则 15 不动手。它照旧是卡片，名下条目照旧是**卡片内条目**（同时也按规则 10 镜像进「🤖 模型」tab）。
- 收窄的直接可见结果：`🧠 推理选项` 层级恢复正常，tab 恢复成 **5 张卡片 + 0 条独立条目**（旧版这里是 4 张卡片 + 3 条独立条目）。
- 面板侧：不再需要那张为了装散条目而临时造出来的「独立条目」假卡片，**已删除**（`src/scripts/40-预设设置.js` 的 buildView；裸放条目现在直接铺成一条网格）。

### 11.3 tab 数：完全不加规则 15 是 8，收窄前后都是 7（层级 6 + 模型 1）

层级一共 8 个，其中 `📌 任务设置` 从来没有卡片/变量、`🪓 神秘咒语` 的唯一卡片被规则 15 判掉 → 出 tab 的层级 6 个，加上全局「🤖 模型」tab，**共 7 个 tab**。收窄**没有改变 tab 数与 tab 顺序**：

| # | tab | 类型 | 卡片数 | 卡片内条目 | 独立条目 | 变量条目 |
|---|---|---|---|---|---|---|
| 1 | 🧩 设置变量 | 层级 tab（order 2 … 8） | 0 | 0 | 2 | 3 |
| 2 | 📖 创作要求 | 层级 tab（order 23 … 64） | 9 | 31 | 0 | 0 |
| 3 | 😀 角色塑造 | 层级 tab（order 65 … 75） | 1 | 8 | 0 | 0 |
| 4 | 📝 写作指导 | 层级 tab（order 76 … 111） | 3 | 31 | 0 | 0 |
| 5 | 🧪 额外任务 | 层级 tab（order 112 … 122） | 3 | 6 | 0 | 0 |
| 6 | 🧠 推理选项 | 层级 tab（order 143 … 162） | 5 | 13 | 0 | 0 |
| 7 | 🤖 模型 | 全局特殊 tab（跨所有层级） | 4 | 12 | 0 | 0 |

- 面板里 tab 的显示顺序按用户裁定改成 **「🤖 模型」排第一个**（其余顺序不变）—— 这是面板侧的事，解析器的 tab 顺序（模型 tab 在最后）不变。
- 消失的是原来的第 2 个层级 tab `🪓 神秘咒语`（它的卡片被规则 15 判掉后，层级变成无卡片无变量）。
- 收窄后**不会再出现**「卡片被降级、层级还出 tab」的情况（那个情况是旧版的副作用 2，本轮随收窄一起消失）。
- tab 里的卡片总数：21 张层级卡片（完全不加规则 15 时是 22 张）+ 4 张模型卡片。

### 11.4 `🪓 神秘咒语` 层级里每条条目的新归属

层级 order 9 … 16，区间内 **6 条**普通条目（两条层级条目本身锁定为开、不在面板）。逐条核对：

| # | order | 条目名 | identifier | 模型归属 | 现在的身份 | 在「🤖 模型」tab 的哪张卡片 | 面板可达 |
|---|---|---|---|---|---|---|---|
| 1 | 10 | 🐱 Gemini随机数 | prompt_1789200146436_lolbt7c | 🐱 | 层级裸放条目（本来就不在任何卡片里） | 🐱 表示Gemini系列模型 | 是（经「🤖 模型」tab） |
| 2 | 11 | 🐱 Gemini抗输入审 | prompt_1789339450008_wci4d02 | 🐱 | 层级裸放条目（本来就不在任何卡片里） | 🐱 表示Gemini系列模型 | 是（经「🤖 模型」tab） |
| 3 | 12 | 🤖 [模型类型] 选一 | prompt_1789213815769_ks7pgcc | - | 被规则 15 降级的**卡片头**（层级裸放条目） | - | **否**（见 11.6） |
| 4 | 13 | 🐋 DS/GLM 💤 | prompt_1788967671203_vigbgmo | 🐋 💤 | 原卡片 🤖 [模型类型] 的名下条目 → 规则 15 后变成层级裸放条目 | 🐋 表示DeepSeek系列模型 / 💤 表示GLM系列模型 | 是（经「🤖 模型」tab） |
| 5 | 14 | 🐱 Gemini | a03f3f0b-0c4d-43a4-aab9-fc34910a7121 | 🐱 | 原卡片 🤖 [模型类型] 的名下条目 → 规则 15 后变成层级裸放条目 | 🐱 表示Gemini系列模型 | 是（经「🤖 模型」tab） |
| 6 | 15 | 🌒 KIMI(占位) | 94df9413-488a-4912-87e3-ec6bd2d345e8 | 🌒 | 原卡片 🤖 [模型类型] 的名下条目 → 规则 15 后变成层级裸放条目 | 🌒 表示Kimi系列模型 | 是（经「🤖 模型」tab） |

- `🐱 Gemini随机数` / `🐱 Gemini抗输入审` 这两条**本来就**是该层级里的裸放条目（不在任何卡片内），规则 15 之后身份不变，仍然**只在「🤖 模型」tab（🐱）里出现**（实测：5 / 6 条在该层级里能找到模型 tab 入口）。
- 被降级的卡片头 + 3 条原卡片名下条目，现在都是该层级的裸放条目；其中 3 条模型条目照旧进模型 tab，卡片头没有模型 emoji，所以它只能在层级里被点到 —— 而层级不出 tab（见 11.6）。

### 11.5 「🤖 模型」tab 的内容

规则 15 **不参与**模型 tab 的收集（那是规则 7/9/10 的事），所以模型 tab 的卡片与条目一个不变。实测：

| 模型卡片 | 条目数 | 条目名（身份） |
|---|---|---|
| 🐋 表示DeepSeek系列模型 | 3 | 🐋 DS/GLM 💤（仅模型 tab） / 🐋 DS/GLM底部破限 💤（镜像） / 🐋 DS/GLM强强 💤（镜像） |
| 💤 表示GLM系列模型 | 3 | 🐋 DS/GLM 💤（仅模型 tab） / 🐋 DS/GLM底部破限 💤（镜像） / 🐋 DS/GLM强强 💤（镜像） |
| 🐱 表示Gemini系列模型 | 5 | 🐱 Gemini随机数（仅模型 tab） / 🐱 Gemini抗输入审（仅模型 tab） / 🐱 Gemini（仅模型 tab） / 🐱 Gemini糖糖（镜像） / 🐱 Gemini卡思维链（镜像） |
| 🌒 表示Kimi系列模型 | 1 | 🌒 KIMI(占位)（仅模型 tab） |

- 唯一模型条目 **9 条**；模型 tab 内出现 **12 次** ＝ 🐋 表示DeepSeek系列模型=3 条 / 💤 表示GLM系列模型=3 条 / 🐱 表示Gemini系列模型=5 条 / 🌒 表示Kimi系列模型=1 条。**「模型 tab 内容有没有变」的答案：没变**（收窄只改卡片身份，不改条目的去留）。
- 「出现次数 = 原生位置 + 模型 tab」= **9 + 12 = 21 次**（含镜像）。
- **本轮（收窄后）的镜像 / 仅模型 tab 拆分：镜像 6 次 / 仅模型 tab 6 次**；完全不加规则 15 时是**镜像 10 次 / 仅模型 tab 2 次**（差在 3 条被降级卡片的条目：它们的 `originCard` 从卡片名变成 `null`）。上一版（未收窄）是镜像 8 次 —— 收窄把 `🤔 [推理格式]` 名下那 2 条还回了卡片，所以镜像次数回升。
- 9 条模型条目里，原生位置能在某个层级 tab 里找到的有 **4 条**（`🐋 DS/GLM底部破限 💤`、`🐋 DS/GLM强强 💤`、`🐱 Gemini糖糖`、`🐱 Gemini卡思维链`）；原生位置在不出 tab 的层级里的有 **5 条**（`🐱 Gemini随机数`、`🐱 Gemini抗输入审`、`🐋 DS/GLM 💤`、`🐱 Gemini`、`🌒 KIMI(占位)`）—— 这 5 条**只靠「🤖 模型」tab 才能被点到**。

### 11.6 「面板上找不到的条目」：4 → 5（**没有回到 4 条，明确报出来**）

用户期望是「回到 4 条（全部来自 `📌 任务设置`）」。**实测是 5 条**：那 4 条 📌 任务设置 之外，还多 1 条 —— `🤖 [模型类型] 选一`（order 12，`prompt_1789213815769_ks7pgcc`）。

**为什么收窄没有救回它**：收窄只保护「不是本层唯一卡片」的卡片，而它是 `🪓 神秘咒语` 层级里**唯一的卡片**（该层卡片数 = 1）→ 规则 15 照旧命中 → 卡片被降级成普通条目 → 而该层级又不出 tab（规则 8）→ 它既不在任何 tab 里、名字里也没有模型 emoji（不进「🤖 模型」tab）、也不是结构区/锁定/marker → 落入「找不到」。

**但它不是有正文的内容条目**：content 共 60 字符，全部是一条 `{{// }}` 注释：`不同模型需要不同的适配方法，条目名有一种emoji的即为该模型专属，选择某一模型时必须关闭其他模型专属条目。`，没有任何正文/指令文本，当前 enabled=开。

| # | order | 条目名 | identifier | content 长度 | content 是否只有一条注释 | 为什么找不到 |
|---|---|---|---|---|---|---|
| 1 | 12 | 🤖 [模型类型] 选一 | prompt_1789213815769_ks7pgcc | 60 | 是（纯注释） | 规则 15 把它的卡片判掉了，层级不出 tab |
| 2 | 18 | 📌 模型任务 | prompt_1788364268110_uiz8qyi | 273 | 否（有正文） | 层级无卡片无变量 → 不出 tab（规则 8，原有问题） |
| 3 | 19 | 📌 身份权限 | prompt_1788364241843_29k9zpo | 698 | 否（有正文） | 层级无卡片无变量 → 不出 tab（规则 8，原有问题） |
| 4 | 20 | 📌 输入解析 | prompt_1788966454790_67nbvel | 320 | 否（有正文） | 层级无卡片无变量 → 不出 tab（规则 8，原有问题） |
| 5 | 21 | 📌 输出规范 | prompt_1788364276043_04lx6bt | 3141 | 否（有正文） | 层级无卡片无变量 → 不出 tab（规则 8，原有问题） |

- 被降级的 3 条模型条目（`🐋 DS/GLM 💤`、`🐱 Gemini`、`🌒 KIMI(占位)`）**没有掉出面板**：它们都在「🤖 模型」tab 里（见 11.4 / 11.5）。
- **结论（报告必须回答的问题）**：`📌 任务设置` 的 4 条仍然是唯一一批「有正文却点不到」的条目（用户已确认接受）；第 5 条是**注释型卡片头**（0 正文），它确实失去了面板入口 → 列为待拍板项（第 10 节第 12 条）。**从面板上消失的「真实内容」条目是 0 条。**

### 11.7 迁移断言复跑

- 本报告生成时已经在内存里跑过同一套迁移断言：**26 / 26 通过**（全部 PASS，无失败）。
- 复跑方式：`src/preset.base.json` 已是迁移后的状态，`migratePreset()` 会短路成「已迁移、0 断言」，所以本报告改用**迁移前备份** `dist/_preset.base.迁移前备份.json`（299126 字符）在内存里重跑迁移，并核对「重跑结果 == 盘上的 base」：**不一致（见第 2 节）**。
- 规则 15 只改解析器（`preset-parse.mjs`）与报告生成器，**没有动** `preset-migrate.mjs`：4 处迁移改动与 26 条断言与本轮之前逐条相同（断言 id 清单可在第 2.2 节核对）。

### 11.8 规则 15（收窄版）的边界与已知副作用

| 项 | 结论 |
| --- | --- |
| 判定输入 | ① 这张卡片是不是它所在层级里唯一的卡片（`r.cards.length === 1`）；② 条目名字里的模型 emoji 归属集合（来自登记表 `📋 支持模型列表 | model` 的 4 个 emoji）。两条都满足才降级 |
| 判定粒度 | 卡片（不是层级、不是条目）；名下**任一**条目无模型归属 → 卡片保留 |
| 命中范围 | 22 张层级卡片里命中 1 张（`🤖 [模型类型]`）；涉及层级 1 个；另有 1 张因「不是本层唯一卡片」被放过（`🤔 [推理格式]`） |
| 与旧版的差 | 旧版（只有条件 ②）会判掉 2 张；收窄后判掉 1 张 → 少判 1 张，正是用户指出的误伤 |
| 条目去留 | **不删条目**：3 条被降级卡片名下的模型条目仍进「🤖 模型」tab；模型 tab 的卡片与条目一个没变（11.5） |
| 可开关条目数 | 完全不加规则 15 时 99 → **99**（本级卡片头降级后掉出面板，不再计入内容条目） |
| 卡片头 / 面板 | 卡片头 22 → 21；面板条目 121 → 120 |
| 副作用（收窄后只剩这一个） | `🪓 神秘咒语` 的卡片头 `🤖 [模型类型] 选一` 失去面板入口（层级不出 tab 且它没有模型 emoji）→ 「找不到的条目」4 → 5（11.6）。它的 content 只有一条注释 |
| 已消除的旧副作用 | 旧版「卡片被降级、层级照旧出 tab → 名下模型条目在层级 tab 与模型 tab 各出现一次（重复展示）」**已随收窄消失**：收窄后只有「唯一卡片的层级」会命中，而那种层级必然不出 tab |
| 顺序 | 层级内顺序不变（卡片头 + 名下条目原地变成裸放条目） |
| 与规则 4 的关系 | 规则 4 管「零条目的卡片不算卡片」，规则 15 是它的补充（条目不为空、但全是模型专属） |
| 与规则 8 的关系 | 规则 8 是出 tab 闸门；规则 15 让这张卡片不再为层级"贡献"卡片，而收窄保证了命中就意味着该层没有别的卡片 → **必然**触发规则 8（本预设里就是 `🪓 神秘咒语`） |
| 面板实现建议 | 被降级的卡片头可用 `cardDemoted` + `cardDemoteReason` 识别；模型 tab 的条目可用 `originCardDemoted` 区分「原卡片被判掉」与「本来就没卡片」；面板已删除自造的「独立条目」假卡片（收窄后不再需要） |

## 12. 复现命令

```
# 迁移检查（默认模式，只报告不写盘）
node test/harness/preset-migrate.mjs
node test/harness/preset-migrate.mjs --check

# 真正写盘（已经执行过：src/preset.base.json 就是迁移后的产物）
node test/harness/preset-migrate.mjs --write

# 重新生成本表（自动跑内存迁移 + 解析；base 已迁移时改用 dist/_preset.base.迁移前备份.json 复跑迁移断言）
node test/harness/preset-report.mjs
```

解析器入口（纯函数，可直接被面板脚本复用）：

```js
import { parsePreset } from "./preset-parse.mjs";
const tree = parsePreset(presetObject);   // 输入：迁移后的预设对象
```

