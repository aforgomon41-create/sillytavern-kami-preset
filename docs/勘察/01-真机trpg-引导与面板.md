# 任务⑥ 外观打磨 · 开工勘察记录（主会话，2026-09-21）

真机：http://localhost:11451/ ｜ 真机当前皮肤 = trpg ｜ 真机当前预设 = **卡密预设0.9-74**（不是 -75，见下）

## 0. 开工前发现的三处「交接.md 与实际不符」

| 交接.md 的说法 | 实测 | 证据 |
|---|---|---|
| `-75` 已部署且是真机当前预设 | 真机当前预设是 **`-74`**；`-75` 只是躺在预设目录里 | `settings.json → oai_settings.preset_settings_openai = "卡密预设0.9-74"`；`OpenAI Settings\卡密预设0.9-74.json` 被酒馆重写过（mtime 2026/9/21 9:38，1364970B），而 `卡密预设0.9-75.json` 与 `dist\` 里的字节数完全一致（1310573B）= 从未被酒馆加载过；真机页面左上角下拉框也显示「卡密预设0.9-74」；引导脚本变量 `kami-guide.seen = 74` |
| `-76`/`-77` 与 `-75` 功能完全一致 | **不一致**：`-76`/`-77` 里带着 `📜 压缩 v0.2` 脚本（`src/scripts/60-压缩.js`，已登记进 `meta.json`）。`-75` 里没有 | `dist\卡密预设0.9-75.json` 搜 `KamiSummarize` = 0 次；`-76`/`-77` = 各 4 次；`-76`/`-77` 体积 1438348B vs `-75` 1310573B |
| `-76`/`-77` 未加放行名单 | `-76` **两道名单都在**；`-77` 两道都不在 | `settings.json → extension_settings.preset_allowed_regex.openai` 含 `卡密预设0.9-76`；`extension_settings.tavern_helper.script.enabled.presets` 也含它 |

**含义**：现在跑 `node build/build.mjs`，新产物会**默认带上 `📜 压缩 v0.2`**（它已在 `meta.json` 里、enabled=true），而这一脚本用户还没在真机验过。任务⑥ 交付前必须先跟用户确认这件事怎么处理。

## 1. 真机 trpg 皮肤下的引导向导（主会话亲眼看，1280x900）

看过的页：1 免责 / 2 选皮肤 / 3 选模型 / 7 创作权限+转述程度。

观察到的问题（待用户确认哪些要改）：

1. **第 2 页四张皮肤卡的预览小窗疑似空白**：四张卡里预览位都是一块平的浅灰米色，看不出该皮肤自己的迷你组件（记事本应是奶白纸、辉光管应是暗金属、恋雨应是深色玻璃，四张却几乎一个色）。同一份预览文档在 `🎨` 面板里是画得出来的（预览台实测，见 `tmp-recon/panel/`）。**已派 subagent 在真机复核（含强制重绘与 🎨 面板对照）**，结论待回。
2. **刊头挤**：`卡密预设 · 新手配置向导` + `第 N / 14 步` 在同一行，紧跟一行 14 个进度点；进度点正好压在纸面纹理的装饰虚线带上，远看像一串脏点。刊头下方到正文之间还有一块明显的空带。
3. **卡片页大片留白**：1280 宽下 `.kami-guide-grid` 是 6 列（180px 起），一组只有 3 张卡时就只占左半屏，右半屏整片空着。截图：第 7 页。
4. **模型页的「当前生效」徽章是冷蓝灰**（`--kami-kind-other`），压在一整套暖纸配色上很跳。截图：第 3 页。
5. **模型页条目卡普遍偏高**，卡里只有一行名字 + 一个 ⓘ，上下留白很大（4 列 × 4 行，滚动量大）。
6. **页脚主按钮是整条深色「墨块」**：第 1 页只有一个按钮时它横跨整屏（约 1100px 宽），份量过重；`上一步` 是透明底，看不出边界。
7. 免责页正文用 `.kami-md`（`--kami-card-2` 实底色）铺成一张大白纸，压在纸面背景上，边界很硬；正文小字与说明字偏灰、偏小。

## 2. 预览台（主会话亲眼看，1280 宽）

- trpg 下 `🌟 预设设置` 面板：7 个 tab 一行放不下（轨道 788px / 可视 420px，可横滚）；条目卡网格 3 列（108px 宽），103 张卡里 38 张开着。
- trpg 下 `🎨 皮肤管理` 面板：皮肤 tab 的三张预览缩略图**画得出来**（迷你组件清晰），第四张（奈亚子的TRPG，即当前皮肤）在可视区外，需要横滚或往下滚。
- 两个聊天前端（手稿 / 手牌）在 trpg 下正常。
- 基线：`node build/lint-skins.mjs` 四套全 PASS；`node build/lint-copy.mjs` 通过。
